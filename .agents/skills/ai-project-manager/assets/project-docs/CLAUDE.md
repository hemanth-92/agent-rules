# Project instructions

Claude Code loads this file automatically and does not read `AGENTS.md` on its
own. The durable instructions live in `AGENTS.md`; the import below pulls them
in, so there is one source of truth for every agent.

@AGENTS.md
